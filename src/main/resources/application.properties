spring.application.name=doc-chunk-pipeline

# S3 Configuration
aws.s3.bucket=my-bucket-name
aws.region=us-east-1

# Kafka Configuration
kafka.bootstrap-servers=localhost:9092
kafka.topic.name=document-chunks

# Chunk Size
chunk.size=1000
